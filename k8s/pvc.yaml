apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: taskflow-data
  namespace: taskflow
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
